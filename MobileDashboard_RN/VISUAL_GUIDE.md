# 📱 React Native Migration - Visual Guide

## Project Overview Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    React Native + Expo App                   │
│                   MobileDashboard_RN/                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
        ┌─────────────────────┼─────────────────────┐
        ↓                     ↓                     ↓
    ┌────────┐         ┌───────────┐         ┌──────────┐
    │   App  │         │Navigation │         │ Screens  │
    │  Entry │◄────────►│  Manager  │◄────────►│  Mgr    │
    │ Point  │         │           │         │          │
    └────────┘         └───────────┘         └──────────┘
        │                                           ↓
        │              ┌──────────────────────┬────────────────┐
        │              ↓                      ↓                ↓
        │          ┌─────────┐         ┌──────────┐      ┌──────────┐
        │          │  Home   │         │ History  │      │Settings  │
        │          │ Screen  │         │ Screen   │      │ Screen   │
        │          └─────────┘         └──────────┘      └──────────┘
        │                                   │                  │
        └──────────────┬────────────────────┼──────────────────┘
                       ↓
        ┌──────────────────────────────────────┐
        │         Components Layer              │
        │  ┌──────────┐ ┌──────────┐          │
        │  │MetricCard│ │Anomaly   │ More... │
        │  └──────────┘ └──────────┘         │
        └──────────────────────────────────────┘
                       ↓
        ┌──────────────────────────────────────┐
        │       State Management (Zustand)     │
        │    ┌─────────────────────────┐      │
        │    │  health.store.ts        │      │
        │    │  • metrics: []          │      │
        │    │  • isLoading: false     │      │
        │    │  • fetchHealthMetrics() │      │
        │    │  • uploadMetric()       │      │
        │    └─────────────────────────┘      │
        └──────────────────────────────────────┘
                       ↓
        ┌──────────────────────────────────────┐
        │        Services Layer                 │
        │  ┌──────────┐ ┌──────────┐          │
        │  │API       │ │Notif.    │ Storage │
        │  │Service   │ │Service   │ Service │
        │  └──────────┘ └──────────┘         │
        └──────────────────────────────────────┘
                       ↓
        ┌──────────────────────────────────────┐
        │      Backend APIs & Services         │
        │  ┌──────────┐ ┌──────────┐          │
        │  │Health    │ │Expo Push │ Storage │
        │  │Data API  │ │Service   │         │
        │  └──────────┘ └──────────┘         │
        └──────────────────────────────────────┘
```

---

## Data Flow Diagram

```
User Interaction
       ↓
┌──────────────────────────┐
│  Component/Screen        │
│  (e.g., HomeScreen)      │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Call Store Action       │
│  (fetchHealthMetrics)    │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Service Layer           │
│  (apiService.get(...))   │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Axios HTTP Client       │
│  + Interceptors          │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Backend API             │
│  (AWS/GCP Endpoint)      │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Response Handling       │
│  + Error Catching        │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Update Store            │
│  (setMetrics())          │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Save to AsyncStorage    │
│  (for offline access)    │
└──────────────────────────┘
       ↓
┌──────────────────────────┐
│  Component Re-renders    │
│  + UI Updates            │
└──────────────────────────┘
       ↓
User sees new data ✅
```

---

## File Organization Hierarchy

```
MobileDashboard_RN/
│
├── 📋 Configuration (Root Level)
│   ├── app.json                  # Expo settings
│   ├── package.json              # Dependencies
│   ├── tsconfig.json             # TypeScript config
│   ├── .eslintrc.js              # Linting
│   └── .env.example              # Environment template
│
├── 📱 Application Files
│   ├── App.tsx                   # Main app shell
│   ├── index.ts                  # Entry point
│   │
│   └── src/                      # Source code
│       │
│       ├── 🎨 config/           # Configuration
│       │   ├── api.config.ts     # API setup (⚙️ EDIT THIS)
│       │   └── theme.config.ts   # Theme colors
│       │
│       ├── 🏪 store/            # State management
│       │   └── health.store.ts   # Zustand store
│       │
│       ├── 🔧 services/         # Business logic
│       │   ├── api.service.ts    # HTTP client
│       │   ├── notification.service.ts
│       │   └── storage.service.ts
│       │
│       ├── 📺 screens/          # Full-screen views
│       │   ├── HomeScreen.tsx
│       │   ├── HistoryScreen.tsx
│       │   └── SettingsScreen.tsx
│       │
│       ├── 🧩 components/       # Reusable pieces
│       │   ├── MetricCard.tsx
│       │   ├── AnomalyAlert.tsx
│       │   ├── Skeleton.tsx
│       │   └── ErrorFallback.tsx
│       │
│       ├── 🗺️ navigation/       # App navigation
│       │   ├── RootNavigator.tsx
│       │   └── BottomTabNavigator.tsx
│       │
│       ├── 📝 types/            # TypeScript defs
│       │   └── index.ts
│       │
│       └── 🛠️ utils/            # Helper functions
│           ├── dateUtils.ts
│           └── numberUtils.ts
│
├── 📚 Documentation
│   ├── QUICK_START.md            # 👈 START HERE
│   ├── README.md
│   ├── MIGRATION_GUIDE_RN.md
│   ├── MIGRATION_COMPLETE.md
│   ├── FILE_INVENTORY.md
│   └── INDEX.md
│
└── 🎨 assets/                    # Images, icons
    ├── images/
    ├── icons/
    └── animations/
```

---

## Feature Implementation Map

```
📱 HOME SCREEN
├── Header
│   ├── Title: "Health Monitor"
│   └── Refresh Button
├── ScrollView + RefreshControl
├── Today's Summary Card
│   ├── Metric Card (HR)
│   ├── Metric Card (Steps)
│   ├── Metric Card (Calories)
│   └── Metric Card (Distance)
├── Anomaly Alert (if anomalies exist)
└── Error Banner (if API error)

🗂️ HISTORY SCREEN
├── Header: "History"
├── SectionList (by date)
├── For each date:
│   └── Metric Items
│       ├── Time
│       ├── Metrics display
│       └── Anomaly badge

⚙️ SETTINGS SCREEN
├── Header: "Settings"
├── Notifications Section
│   └── Toggle switch
├── Display Section
│   └── Dark mode toggle
├── Sync Section
│   └── WiFi-only toggle
├── Storage Section
│   ├── Cache size display
│   └── Clear button
└── About Section
    ├── Version
    └── Build number
```

---

## Component Dependencies

```
App.tsx
  ↓
RootNavigator
  ↓
BottomTabNavigator
  ├─→ HomeScreen
  │   ├─→ useHealthStore() [Zustand]
  │   ├─→ MetricCard (component)
  │   ├─→ AnomalyAlert (component)
  │   ├─→ Skeleton (component)
  │   └─→ ErrorFallback (component)
  │
  ├─→ HistoryScreen
  │   ├─→ useHealthStore() [Zustand]
  │   └─→ SectionList
  │
  └─→ SettingsScreen
      ├─→ storageService
      └─→ useHealthStore()
```

---

## Data Flow for Health Metrics

```
┌────────────────────────────────────────────────┐
│ HomeScreen mounts → useEffect hook              │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ fetchHealthMetrics() from store                │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Set isLoading = true, clear error              │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ apiService.getHealthMetrics()                  │
│ axios.get('/health/metrics')                   │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Interceptor adds auth token & logs             │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Backend responds with metrics array            │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Response interceptor logs success              │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Store processes data:                          │
│  • Sort by timestamp (newest first)            │
│  • Extract anomalies                           │
│  • Extract today's metrics                     │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Save to AsyncStorage for offline access       │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ setIsLoading = false, setError = null          │
└────────────────────────────────────────────────┘
                        ↓
┌────────────────────────────────────────────────┐
│ Component re-renders with new data             │
│ Shows metrics, anomalies, or error             │
└────────────────────────────────────────────────┘
                        ↓
User sees updated health information ✅
```

---

## Configuration Point Map

```
CRITICAL EDITS NEEDED:
│
├── src/config/api.config.ts
│   ├── baseUrl ⚙️ MUST EDIT
│   └── apiKey ⚙️ MUST EDIT
│
├── app.json
│   ├── name (App name)
│   └── version (App version)
│
└── .env
    ├── Copy from .env.example
    └── Fill in your values

OPTIONAL CUSTOMIZATIONS:
│
├── src/config/theme.config.ts
│   ├── Colors
│   ├── Spacing
│   └── Typography
│
├── src/screens/*.tsx
│   └── UI text & layout
│
└── assets/
    ├── App icon
    ├── Splash screen
    └── Custom images
```

---

## Technology Stack Visualization

```
┌─────────────────────────────────────────────────┐
│         Application Layer (React)               │
│  Components, Hooks, Functional Programming     │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│    Framework Layer (React Native)               │
│  Native UI components, platform abstractions   │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│   Development Platform (Expo)                   │
│  DevTools, debugging, build tooling            │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│   State Management (Zustand)                    │
│  Global state, actions, subscriptions          │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│   HTTP Layer (Axios)                            │
│  Requests, interceptors, error handling        │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│   Native Modules                                │
│  AsyncStorage, Notifications, NetInfo          │
└─────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────┐
│   Native Code (iOS/Android)                     │
│  Bridge between JS and native platforms        │
└─────────────────────────────────────────────────┘
```

---

## Development Workflow

```
1. Code Change
   ↓
2. npm start (if not running)
   ↓
3. Hot Reload (automatic in most cases)
   ↓
4. See changes in app
   ↓
5. Check console for errors
   ↓
6. Debug with Expo DevTools
   ↓
7. Test on device/simulator
   ↓
8. Commit changes
   ↓
9. Repeat!
```

---

## Common Commands Quick Reference

```
Development
├── npm start              → Start dev server
├── npm run ios            → iOS Simulator
├── npm run android        → Android Emulator
├── npm run web            → Web Browser
└── (Press 'r' to reload)

Code Quality
├── npm run lint           → Check code style
├── npm run type-check     → TypeScript validation
└── npm test               → Run tests

Building
├── npm run build          → Production build
├── eas build --platform ios      → EAS build iOS
└── eas build --platform android  → EAS build Android
```

---

## Deployment Path

```
Local Development
       ↓
   Testing
       ↓
   EAS Build (iOS)
       ↓
   TestFlight
       ↓
   App Store
       ↓
   Production (iOS)


   Local Development
       ↓
   Testing
       ↓
   EAS Build (Android)
       ↓
   Internal Testing
       ↓
   Google Play
       ↓
   Production (Android)
```

---

## Success Milestones

```
Milestone 1: ✅ Setup Complete
└─ npm install successful
└─ API configured
└─ No TypeScript errors

Milestone 2: ✅ App Runs
└─ npm start works
└─ App opens in simulator
└─ No crashes

Milestone 3: ✅ Features Work
└─ Metrics display
└─ Navigation works
└─ Pull-to-refresh functions

Milestone 4: ✅ Ready to Deploy
└─ All screens tested
└─ Notifications working
└─ Error handling verified

Milestone 5: ✅ In Production
└─ Built and signed
└─ Submitted to stores
└─ Users can install
```

---

**Last Updated:** January 9, 2026  
**Status:** ✅ All Diagrams Complete  

*Use these visualizations as reference while developing!*
